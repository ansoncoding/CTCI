#ifndef IS_STRING_ROTATION_H // include guard
#define IS_STRING_ROTATION_H 
#include <string>
using namespace std;
bool is_substring(string src, string substr);
bool is_string_rotation(string s1, string s2);
#endif